<Window x:Class="DM.App.Views.Dialogs.ClipboardOfferPopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Width="360" Height="90"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="Manual">

    <Window.Resources>
        <Storyboard x:Key="SlideIn">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                             From="0" To="1" Duration="0:0:0.18"/>
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                             From="20" To="0" Duration="0:0:0.18">
                <DoubleAnimation.EasingFunction><QuarticEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
    </Window.Resources>

    <!-- Outer container: shadow + rounded card -->
    <Border x:Name="Root" CornerRadius="10" Opacity="0"
            Background="#F0202020"
            BorderBrush="#40FFFFFF" BorderThickness="1">
        <Border.RenderTransform><TranslateTransform/></Border.RenderTransform>
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="18" ShadowDepth="4" Opacity="0.55"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="4"/><!-- countdown bar -->
            </Grid.RowDefinitions>

            <!-- Content row -->
            <Grid Grid.Row="0" Margin="12,10,12,6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Icon -->
                <ui:SymbolIcon Grid.Column="0" Symbol="ArrowCircleDown24"
                               FontSize="22" Foreground="#FF7EC8E3"
                               VerticalAlignment="Center" Margin="0,0,12,0"/>

                <!-- Text -->
                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock x:Name="TitleText"
                               FontSize="12" FontWeight="SemiBold"
                               Foreground="White"
                               TextTrimming="CharacterEllipsis"/>
                    <TextBlock x:Name="UrlText"
                               FontSize="10.5" Foreground="White" Opacity="0.50"
                               TextTrimming="CharacterEllipsis" Margin="0,2,0,0"/>
                </StackPanel>

                <!-- Buttons -->
                <StackPanel Grid.Column="2" Orientation="Horizontal"
                            VerticalAlignment="Center" Margin="10,0,0,0">
                    <ui:Button x:Name="DownloadBtn"
                               Appearance="Primary" Padding="12,5"
                               FontSize="12" Margin="0,0,6,0"
                               Click="DownloadBtn_Click">Download</ui:Button>
                    <Button Width="26" Height="26" Padding="0"
                            Background="Transparent" BorderThickness="0"
                            Cursor="Hand" ToolTip="Dismiss"
                            Click="DismissBtn_Click">
                        <ui:SymbolIcon Symbol="Dismiss24" FontSize="11"
                                       Foreground="White" Opacity="0.55"/>
                    </Button>
                </StackPanel>
            </Grid>

            <!-- Countdown progress bar -->
            <Border Grid.Row="1" CornerRadius="0,0,10,10" Background="#28FFFFFF">
                <ProgressBar x:Name="CountdownBar"
                             Minimum="0" Maximum="1" Value="1"
                             Height="4" Background="Transparent"
                             BorderThickness="0">
                    <ProgressBar.Foreground>
                        <SolidColorBrush Color="#FF7EC8E3"/>
                    </ProgressBar.Foreground>
                </ProgressBar>
            </Border>
        </Grid>
    </Border>
</Window>
